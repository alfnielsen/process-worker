{
  "name": "redis-process-worker",
  "description": "A worker process helper for using redis cache and streams.",
  "version": "0.0.1beta",
  "module": "src/ProcessWorker.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build src/ProcessWorker.ts --outdir dist --target node"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "redis": "^5.5.6"
  },
  "peerDependencies": {
    "typescript": "^5",
    "redis": "^5.5.6"
  }
}
